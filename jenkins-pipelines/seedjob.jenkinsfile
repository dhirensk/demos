pipelineJob("pipeline_example1"){
    definition {
        cps {
            sandbox(true)
            script(streamFileFromWorkspace('jenkins-pipelines/pipeline1.jenkinsfile'))
        }
    }
}
pipelineJob("pipeline_example2"){
    definition {
        cps {
            sandbox(true)
            script(streamFileFromWorkspace('jenkins-pipelines/pipeline2.jenkinsfile'))
        }
    }
}

