pipelineJob("pipeline_example1"){
    definition {
        cps {
            sandbox(true)
            script(streamFileFromWorkspace('jenkins-pipeline/pipeline1.jenkinsfile'))
        }
    }
}
pipelineJob("pipeline_example2"){
    definition {
        cps {
            sandbox(true)
            script(streamFileFromWorkspace('jenkins-pipeline/pipeline2.jenkinsfile'))
        }
    }
}

